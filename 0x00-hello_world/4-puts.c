#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char MyPuts[] = "\"Programming is like building a multilingual puzzle";

	puts(MyPuts);
	return (0);
}
