#include "holberton.h"
/**
 * add - add two integers and returns the result
 * @uno: first number input
 * @deux: second number input
 * Return: uno + deux
 */
int add(int uno, int deux)
{
	return (uno + deux);
}
